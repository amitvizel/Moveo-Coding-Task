# ============================================================================
# CLIENT ENVIRONMENT VARIABLES
# ============================================================================
# Copy these to client/.env

# API Server URL
# For development: http://localhost:3000
# For production: your server URL (e.g., https://your-server.onrender.com)
VITE_API_URL=http://localhost:3000

# ============================================================================
# SERVER ENVIRONMENT VARIABLES
# ============================================================================
# Copy these to server/.env

# Required environment variables
JWT_SECRET=YOUR_SECRET_KEY_HERE

# CORS configuration
# For development: single origin
# CORS_ORIGIN=http://localhost:5173
# For production: comma-separated list of allowed origins
# CORS_ORIGIN=https://your-client.vercel.app,http://localhost:5173
# Optional - defaults to http://localhost:5173 if not set
CORS_ORIGIN=http://localhost:5173

# Server port
# Optional - defaults to 3000 if not set
# Note: Render automatically sets PORT, no need to configure in production
PORT=3000

# Database configuration
# Development (local Docker)
DATABASE_URL="postgresql://postgres:password@localhost:5432/crypto_advisor?schema=public"
# Production (Render PostgreSQL)
# DATABASE_URL is automatically provided by Render when using Render PostgreSQL service
# Format: postgresql://user:password@host:port/database?schema=public

# Optional API keys (app will work without them but with limited functionality)
COINGECKO_API_KEY=your_coingecko_demo_api_key_here
CRYPTOPANIC_API_KEY=your_key
HUGGINGFACE_API_KEY=your_huggingface_token_here

# ============================================================================
# Render Deployment Configuration
# ============================================================================
# 
# Build Command: npm install && npm run build
# Start Command: npm start
#
# Required Environment Variables in Render:
# - DATABASE_URL: Automatically provided if using Render PostgreSQL
# - JWT_SECRET: Generate a strong secret key (e.g., use openssl rand -base64 32)
# - CORS_ORIGIN: Your frontend URL (e.g., https://your-client.vercel.app)
#   For multiple origins, use comma-separated: https://app1.com,https://app2.com
#
# Optional Environment Variables:
# - COINGECKO_API_KEY: For CoinGecko Demo API (recommended for better rate limits)
# - CRYPTOPANIC_API_KEY: For crypto news functionality
# - HUGGINGFACE_API_KEY: For AI insights functionality
# - PORT: Automatically set by Render (do not configure manually)
#
# Database Migrations:
# Migrations run automatically via the postbuild script (prisma migrate deploy)
# Make sure your DATABASE_URL is set before the build completes.
